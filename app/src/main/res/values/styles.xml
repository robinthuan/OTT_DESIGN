<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="CustomActivatedColor" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/edittext_nomal</item>
        <item name="colorControlActivated">@color/color_background_button</item>
        <item name="editTextColor">@color/color_background_button</item>
    </style>
</resources>
